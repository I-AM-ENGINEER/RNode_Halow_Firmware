Очереди / инфраструктура

lmac_lmac_rxq.o.S — RX-очередь LMAC: инициализация и операции put/get/free/count (буферизация принятых кадров/дескрипторов).

Сон / пробуждение

mars_ah_dsleep.o.S — sleep/deepsleep: конфиг сна, вход в сон, resume, системный reset, связанные сервисные функции питания/пробуждения.

PHY (цифровая часть физики)

mars_ah_wphy.o.S — конфигурация Wi-Fi PHY: режимы RX gain, компенсации (imbalance), выбор/проверка DPD RAM, прочие настройки трактов PHY.

mars_ah_wphy_parameter_v2.o.S — таблицы параметров PHY (v2) (в этом дампе дизасма кода нет — по сути blob/данные калибровок/профилей).

RF (аналог/калибровки)

mars_ahrf.o.S — RF-калибровки: DCOC (DC offset), ADC cal, IQ imbalance компенсация и связанные процедуры.

mars_ahrf_parameter.o.S — таблицы параметров RF (данные/профили, без кода в этом выводе).

mars_ahrf_iq_curve.o.S — IQ curve / кривые коррекции (табличные данные, без кода в этом выводе).

mars_rfdigicali.o.S — управление digital RF calibration: старт/стоп калибровок, power calc, hw-trigger для RX DCOC и т.п.

mars_rfspi.o.S — драйвер/обвязка RF SPI: пины, частота SPI, задержки RX, режимы триггера, open/enable.

Debug/Test path (внутренние тестовые маршруты)

mars_dbgpath.o.S — настройка dbgpath (тестовые/отладочные mux’ы/маршруты): задержки TX/RX, extspi-параметры (tx power/rx gain и т.д.).

mars_dbgpath_parameter_v2.o.S — параметры dbgpath (v2) (табличные данные, без кода в этом выводе).

LMAC: “мозги” и основные пайплайны

mars_lmac.o.S — “ядро” LMAC: обработчики wakeup/irq, задачи/таймеры, коллбеки скана/проб и т.п. (склейка логики).

mars_lmac_cfg.o.S — конфигурация MAC: CW/AIFS/TXOP/AID и прочие runtime-параметры.

mars_lmac_hw.o.S — низкий уровень управления MAC-железом: enable IRQ по AC, старт RX/CCA, abort FSM, чтение статусов/таймингов.

mars_lmac_rx.o.S — RX-пайплайн: разбор принятых кадров (в т.ч. NDP/paging/ctrl/data обработчики).

mars_lmac_tx.o.S — TX-пайплайн: сборка/формирование кадров (mgmt/data/ctrl), подготовка полей, сервисные put_unaligned и т.п.

mars_lmac_rc.o.S — rate control: выбор/обновление скоростей, обработка tx status, состояние антенн.

mars_lmac_cipher.o.S — крипто-движок (CE): init/start + IRQ handler (шифрование/дешифрование MAC-уровня, типа CCMP/TKIP — по смыслу).

mars_lmac_util.o.S — утилиты LMAC: задержки (delay_us), обновление PSM-инфо, отладочный вывод (hex/char).

mars_lmac_test.o.S — тестовый режим: enable test, подготовка TX SKB для теста, проверки LO freq, чтение temp sensor, “полевые” хелперы.

TDMA

mars_tdma.o.S — управление TDMA: режим, IRQ, буферы, длины триггеров, старт/стоп.